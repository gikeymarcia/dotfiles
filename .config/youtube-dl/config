# FILENAME
#-o ./%(uploader)s--%(id)s.%(ext)s
-o ./%(uploader)s--%(id)s--%(title)s.%(ext)s

# always settings
--ignore-errors
--restrict-filenames
--playlist-random

# max quality
--format "best[height<=720]"

# audio only
#-x
#--audio-format mp3
